import React from 'react';
import Layout from '../components/layout';

const WebhookLayout = () => {
  return (
    <Layout>
      {/* Add webhook subscription UI here -- currently empty placeholder -->
      <div class="webhook-section">
        <h2>Webhook Subscription</h2>
        {/* Example webhook input fields -->
        <input type="text" placeholder="Webhook URL"/>
        <button>Subscribe</button>
      </div>
      */}
    </Layout>
  )
}

export default WebhookLayout;